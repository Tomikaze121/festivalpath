import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gitmenalang.festivalpath',
  appName: 'festivalpath',
  webDir: 'www'
};

export default config;
